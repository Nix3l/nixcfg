{ ... }:

{
    home.stateVersion = "25.05";
    hm.mods = {
        defaults.enable = true;
        alacritty.enable = true;
        git.enable = true;

        nvim = {
            enable = true;
            discord = true;
        };

        spotify.enable = true;

        gtk = {
            enable = true;
            gruvbox.enable = true;
        };

        qt.enable = true;

        zsh.enable = true;
    };
}
